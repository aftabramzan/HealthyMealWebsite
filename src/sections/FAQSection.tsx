import { useState } from 'react';
import { Pressable, Text, View } from 'react-native';
import { faqs } from '../data/mockData';
import { FAQAccordion } from '../components/FAQAccordion';
export function FAQSection() { const [openId, setOpenId] = useState<string | null>(null); return <View className="bg-cream px-6 py-16 md:px-10 md:py-20"><View className="mx-auto w-full max-w-6xl flex-col gap-10 md:flex-row md:gap-20"><View className="w-full md:w-[38%]"><Text className="mb-3 text-[10px] font-bold uppercase tracking-[2px] text-olive">Frequently Asked Questions</Text><Text className="font-display text-4xl leading-[1.05] text-ink">Have Questions?{`\n`}We’ve Got Answers.</Text><Text className="mt-4 text-sm leading-6 text-gray-600">Find quick answers to common questions about our meal plans, delivery, and more.</Text><Pressable className="mt-7 self-start rounded-full bg-olive px-6 py-4"><Text className="text-sm font-bold text-white">View All FAQs  →</Text></Pressable></View><View className="w-full md:w-[62%]">{faqs.map((faq) => <FAQAccordion key={faq.id} question={faq.question} answer={faq.answer} isOpen={openId === faq.id} onPress={() => setOpenId(openId === faq.id ? null : faq.id)} />)}</View></View></View>; }
