import { Pressable, Text, View } from 'react-native';
import { SectionHeader } from '../components/SectionHeader';
import { goToSection } from '../utils/navigation';
const steps = [['01', 'Choose Your Plan', 'Select the meal plan that fits your goals.'], ['02', 'We Prepare Fresh Meals', 'Our chefs prepare nutritious meals with care.'], ['03', 'Enjoy Convenient Delivery', 'Receive your meals and eat a healthier you.']];
export function ProcessSection() { return <View className="bg-sage px-6 py-14 md:px-10 md:py-16"><View className="mx-auto w-full max-w-6xl"><View className="flex-row items-end justify-between"><SectionHeader eyebrow="Our Advantages" title="Healthy Eating in 3 Simple Steps" /><Pressable onPress={() => goToSection('contact')} className="hidden md:flex" accessibilityRole="button"><Text className="text-xs font-bold text-olive">It's Easy to Get Started  →</Text></Pressable></View><View className="mt-8 grid grid-cols-1 gap-7 md:grid-cols-3">{steps.map(([number, title, text], index) => <View key={number} className="flex-row gap-4 md:items-start"><View className="items-center"><Text className="h-10 w-10 rounded-full bg-white text-center text-lg font-bold leading-10 text-olive">{number}</Text>{index < steps.length - 1 ? <Text className="hidden pt-2 text-xl text-olive md:flex">→</Text> : null}</View><View className="flex-1"><Text className="font-bold text-ink">{title}</Text><Text className="mt-2 text-sm leading-5 text-gray-500">{text}</Text></View></View>)}</View></View></View>; }
