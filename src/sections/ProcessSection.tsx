import { MaterialCommunityIcons } from '@expo/vector-icons';
import { Pressable, Text, View } from 'react-native';
import { SectionHeader } from '../components/SectionHeader';
import { goToSection } from '../utils/navigation';
const steps = [['01', 'Choose Your Plan', 'Select the meal plan that fits your goals.'], ['02', 'We Prepare Fresh Meals', 'Our chefs prepare nutritious meals with care.'], ['03', 'Enjoy Convenient Delivery', 'Receive your meals and eat a healthier you.']];
export function ProcessSection() { return <View className="bg-sage px-6 py-14 md:px-10 md:py-16"><View className="mx-auto w-full max-w-6xl"><View className="flex-row items-end justify-between"><SectionHeader eyebrow="Our Advantages" title="Healthy Eating in 3 Simple Steps" /><Pressable onPress={() => goToSection('contact')} className="hidden md:flex" accessibilityRole="button"><Text className="text-xs font-bold text-olive">It's Easy to Get Started  →</Text></Pressable></View><View className="mt-8 grid grid-cols-1 gap-7 md:grid-cols-3">{steps.map(([number, title, text], index) => <View key={number} className="flex-row items-start gap-4"><Text className="h-10 w-10 rounded-full bg-white text-center text-lg font-bold leading-10 text-olive">{number}</Text><View className="flex-1"><View className="flex-row items-center gap-3">{index === 0 ? <MaterialCommunityIcons name="account-heart-outline" size={25} color="#60723b" accessibilityLabel="Choose your plan" /> : index === 1 ? <MaterialCommunityIcons name="bowl-mix-outline" size={27} color="#60723b" accessibilityLabel="Fresh meal preparation" /> : <MaterialCommunityIcons name="truck-delivery-outline" size={26} color="#60723b" accessibilityLabel="Delivery" />}<Text className="font-bold text-ink">{title}</Text></View><Text className="mt-2 text-sm leading-5 text-gray-500">{text}</Text></View></View>)}</View></View></View>; }
