# Healthy Meals

A responsive Healthy Meals landing page built with Expo, React Native for Web, TypeScript, and NativeWind.

## Run locally

```bash
npm install
npm run web
```

## Production build

```bash
npm run build
```

The generated `dist` folder is configured for Vercel with `vercel.json`. No environment variables are required.
